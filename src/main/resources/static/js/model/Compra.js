class Compra {

    constructor(id, descricao, dataCompra, fornecedor, compraProdutos) {
        this.id = id;
        this.descricao = descricao;
        this.dataCompra = dataCompra;
        this.fornecedor = fornecedor;
        this.compraProdutos = compraProdutos;
    }
}